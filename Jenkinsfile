pipeline {
    agent any 
    
    stages {
        stage('dev') {
            steps {
                echo 'my pipeline execution: building the application in dev environment...'
            }
        }
    }
}
